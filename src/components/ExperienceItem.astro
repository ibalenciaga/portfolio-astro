---
const {date, title, description} = Astro.props;
---
<div class="absolute w-3 h-3 bg-sky-400 rounded-full mt-1.5 -start-1.5 border border-white dark:border-gray-900 dark:bg-gray-700"></div>
<div class="md:flex md:flex-row">
    <div class="md:flex md:flex-col min-w-[300px]">
        <time class="text-5xl md:text-sm mb-5 md:m-2 font-normal leading-none">{date}</time>
        <h3 class="text-6xl md:text-xl mb-5 md:m-2 font-semibold text-sky-400">{title}</h3>
    </div>
    <div class="">
        <h4 class="text-5xl md:text-base mb-5 md:m-2 font-normal">{description}</h4>
    </div>
</div>
