---
import ExperienceItem from '../components/ExperienceItem.astro'
import BriefcaseIcon from './icons/BriefcaseIcon.astro'

import { EXPERIENCE } from '../store/experienceStore.ts'
---

<section id="experience" class="flex gap-20 justify-center mb-24 ps-24 pe-24">
    <div class="w-full justify-center lg:w-[800px]">
        <h2 class="mb-10 flex flex-row justify-center items-center">
            <span class="text-6xl md:text-5xl ms-2 font-bold text-cyan-400 drop-shadow-[0_0_15px_rgba(6,182,212,0.8)]">Experiencia</span>
        </h2>
        <ol class="relative border-s-2 border-cyan-500/30">
            {
                EXPERIENCE.map((experience) => (
                    <li class={`ms-4 mb-32 sm:mb-10 text-left transition ${experience?.show}`}>
                        <ExperienceItem {...experience} />
                    </li>
                ))
            }
        </ol>
        {}
        <button
            id="showMoreButton"
            class="bg-gradient-to-r from-cyan-500 to-blue-600 text-gray-900 font-bold px-6 py-3 rounded-lg text-5xl md:text-sm hover:from-cyan-400 hover:to-blue-500 transition-all duration-300 shadow-[0_0_20px_rgba(6,182,212,0.6)] hover:shadow-[0_0_30px_rgba(6,182,212,0.8)] hover:scale-105"
            >Ver más</button
        >
    </div>
</section>

<script type="module">
    import { toggleExperiences } from '../../public/functions.js'

    document.querySelector('#showMoreButton').addEventListener('click', toggleExperiences)
</script>
